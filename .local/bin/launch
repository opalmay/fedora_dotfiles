#!/bin/sh

# bspc rule -a Brave-browser desktop=DP-1:^1 follow=off -o
# gtk-launch ~/.local/share/applications/brave-outlook.desktop & disown
#
# bspc rule -a teams-for-linux desktop=DP-1:^2 follow=off -o
# flatpak run com.github.IsmaelMartinez.teams_for_linux & disown
#
# bspc rule -a remmina desktop=DP-1:^3 follow=off -o
# remmina & disown
#
bspc rule -a "Apache Directory Studio" desktop=DP-1:^4 follow=off -o
/opt/ApacheDirectoryStudio/ApacheDirectoryStudio & disown

# bspc rule -a KeePassXC desktop=DP-1:^6 follow=off -o
# keepassxc & disown

# brave-browser & disown
